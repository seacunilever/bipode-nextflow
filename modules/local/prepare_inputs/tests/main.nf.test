nextflow_process {

    name "Test Process PREPARE_INPUTS"
    script "modules/local/prepare_inputs/main.nf"
    process "PREPARE_INPUTS"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = file("${projectDir}/assets/test_data/minimal/Example_meta_data.csv")
                input[1] = file("${projectDir}/assets/test_data/minimal/sers_meta_data_mapper.yml")
                input[2] = file("${projectDir}/assets/test_data/minimal/Example_counts_5probes.csv")
                input[3] = file("${projectDir}/assets/test_data/minimal/substances_cell_types.yml")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
